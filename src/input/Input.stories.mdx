import { Meta, Story, Canvas } from '@storybook/addon-docs';

import './index';


<Meta title="Example/Input" />


export const Template = ({ primary, label }) => <wheat-input primary={primary} label={label}></wheat-input>;

<Story
  name="Primary"
  args={{
    primary: true,
    label: 'Button',
  }}>
  {Template.bind({})}
</Story>